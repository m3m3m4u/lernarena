export interface Point { x: number; y: number; }
export interface QuestionBlock { question: string; answers: string[]; correct: number; }
export interface Food { x:number; y:number; color:string; answer:string; correct:boolean; }
